name: frameforge-ai-prod

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --innodb-buffer-pool-size=256M
      - --max-connections=50
      - --performance-schema=OFF
    volumes:
      - frameforge_prod_mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -u root -p$$MYSQL_ROOT_PASSWORD"]
      interval: 5s
      timeout: 5s
      retries: 30
    mem_limit: 1280m
    cpus: 0.6
    logging: *default-logging

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command:
      - sh
      - -c
      - exec redis-server --appendonly yes --requirepass "$$REDIS_PASSWORD" --maxmemory 192mb --maxmemory-policy noeviction
    environment:
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - frameforge_prod_redis:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep PONG"]
      interval: 5s
      timeout: 3s
      retries: 20
    mem_limit: 256m
    cpus: 0.2
    logging: *default-logging

  qdrant:
    image: qdrant/qdrant:v1.18.3
    restart: unless-stopped
    volumes:
      - frameforge_prod_qdrant:/qdrant/storage
    mem_limit: 768m
    cpus: 0.5
    logging: *default-logging

  rmqinit:
    image: apache/rocketmq:4.9.4
    user: "0:0"
    command: ["sh", "-c", "chown -R 3000:3000 /namesrv-logs /broker-logs /store"]
    restart: "no"
    volumes:
      - frameforge_prod_rmq_namesrv_logs:/namesrv-logs
      - frameforge_prod_rmq_broker_logs:/broker-logs
      - frameforge_prod_rmq_store:/store
    mem_limit: 64m
    cpus: 0.1
    logging: *default-logging

  rmqnamesrv:
    image: apache/rocketmq:4.9.4
    restart: unless-stopped
    command: sh mqnamesrv
    environment:
      JAVA_OPT_EXT: "-Xms128m -Xmx256m -Xmn96m"
    volumes:
      - frameforge_prod_rmq_namesrv_logs:/home/rocketmq/logs
    depends_on:
      rmqinit:
        condition: service_completed_successfully
    mem_limit: 384m
    cpus: 0.25
    logging: *default-logging

  rmqbroker:
    image: apache/rocketmq:4.9.4
    restart: unless-stopped
    command: sh mqbroker -n rmqnamesrv:9876
    environment:
      NAMESRV_ADDR: rmqnamesrv:9876
      JAVA_OPT_EXT: "-Xms256m -Xmx768m -Xmn256m"
    depends_on:
      rmqinit:
        condition: service_completed_successfully
      rmqnamesrv:
        condition: service_started
    volumes:
      - frameforge_prod_rmq_store:/home/rocketmq/store
      - frameforge_prod_rmq_broker_logs:/home/rocketmq/logs
    mem_limit: 1024m
    cpus: 0.5
    logging: *default-logging

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        DEBIAN_MIRROR: ${DEBIAN_MIRROR:-deb.debian.org}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      ROCKETMQ_NAMESERVER: ${ROCKETMQ_NAMESERVER}
      ROCKETMQ_TOPIC: ${ROCKETMQ_TOPIC}
      ROCKETMQ_PRODUCER_GROUP: ${ROCKETMQ_PRODUCER_GROUP}
      BILIBILI_IMPORT_ENABLED: ${BILIBILI_IMPORT_ENABLED}
      BILIBILI_MAX_DURATION_SECONDS: ${BILIBILI_MAX_DURATION_SECONDS}
      BILIBILI_MAX_FILE_BYTES: ${BILIBILI_MAX_FILE_BYTES}
      BILIBILI_IMPORT_ATTEMPTS: ${BILIBILI_IMPORT_ATTEMPTS}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRE_HOURS: ${JWT_EXPIRE_HOURS}
      APP_ENV: production
      AUTO_CREATE_SCHEMA: "false"
      ROOT_PATH: /api
      UPLOAD_ROOT: /data/uploads
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      MAX_CHUNK_BYTES: ${MAX_CHUNK_BYTES}
      MAX_UPLOAD_BYTES: ${MAX_UPLOAD_BYTES}
      MAX_VIDEO_DURATION_SECONDS: ${MAX_VIDEO_DURATION_SECONDS}
      VALIDATE_VIDEO_CONTENT: ${VALIDATE_VIDEO_CONTENT}
      UPLOAD_SESSION_TTL_HOURS: ${UPLOAD_SESSION_TTL_HOURS}
      MAX_ANALYSIS_ATTEMPTS: ${MAX_ANALYSIS_ATTEMPTS}
      STALE_TASK_SECONDS: ${STALE_TASK_SECONDS}
      QUEUED_TASK_FALLBACK_SECONDS: ${QUEUED_TASK_FALLBACK_SECONDS:-10}
      AI_BASE_URL: ${AI_BASE_URL}
      AI_API_KEY: ${AI_API_KEY}
      AI_MODEL: ${AI_MODEL}
      AI_THINKING_MODE: ${AI_THINKING_MODE:-disabled}
      AI_REQUEST_TIMEOUT_SECONDS: ${AI_REQUEST_TIMEOUT_SECONDS:-180}
      AI_INPUT_COST_PER_MILLION_TOKENS: ${AI_INPUT_COST_PER_MILLION_TOKENS:-0}
      AI_OUTPUT_COST_PER_MILLION_TOKENS: ${AI_OUTPUT_COST_PER_MILLION_TOKENS:-0}
      AI_CACHE_HIT_INPUT_COST_PER_MILLION_TOKENS: ${AI_CACHE_HIT_INPUT_COST_PER_MILLION_TOKENS:-0}
      AGENT_MAX_TOOL_STEPS: ${AGENT_MAX_TOOL_STEPS}
      AGENT_MEMORY_MAX_MESSAGES: ${AGENT_MEMORY_MAX_MESSAGES:-12}
      AGENT_HISTORY_MAX_MESSAGES: ${AGENT_HISTORY_MAX_MESSAGES:-200}
      AGENT_PIPELINE_VERSION: ${AGENT_PIPELINE_VERSION:-structured-v5}
      EVIDENCE_RETRIEVER_PROFILE: ${EVIDENCE_RETRIEVER_PROFILE:-coverage-aware-qdrant-hybrid-v2}
      EVIDENCE_RETRIEVAL_FALLBACK_ENABLED: ${EVIDENCE_RETRIEVAL_FALLBACK_ENABLED:-true}
      EVIDENCE_ABSTENTION_ENABLED: ${EVIDENCE_ABSTENTION_ENABLED:-true}
      EVIDENCE_MIN_DENSE_SCORE: ${EVIDENCE_MIN_DENSE_SCORE:-0.45}
      EVIDENCE_MIN_LEXICAL_SCORE: ${EVIDENCE_MIN_LEXICAL_SCORE:-0.18}
      EVIDENCE_EMBEDDING_MODEL_DIR: ${EVIDENCE_EMBEDDING_MODEL_DIR:-/data/models/retrieval/bge-small-zh-v1.5}
      EVIDENCE_EMBEDDING_ALLOW_DOWNLOAD: ${EVIDENCE_EMBEDDING_ALLOW_DOWNLOAD:-true}
      EVIDENCE_EMBEDDING_CPU_THREADS: ${EVIDENCE_EMBEDDING_CPU_THREADS:-2}
      EVIDENCE_EMBEDDING_BATCH_SIZE: ${EVIDENCE_EMBEDDING_BATCH_SIZE:-32}
      QDRANT_URL: ${QDRANT_URL:-http://qdrant:6333}
      QDRANT_API_KEY: ${QDRANT_API_KEY:-}
      QDRANT_COLLECTION: ${QDRANT_COLLECTION:-frameforge_evidence_v1}
      QDRANT_TIMEOUT_SECONDS: ${QDRANT_TIMEOUT_SECONDS:-10}
      ASR_BASE_URL: ${ASR_BASE_URL}
      ASR_API_KEY: ${ASR_API_KEY}
      ASR_MODEL: ${ASR_MODEL}
      OCR_ENABLED: "false"
      LOCAL_EXECUTOR_WORKERS: 1
      LOG_LEVEL: ${LOG_LEVEL}
    volumes:
      - frameforge_prod_uploads:/data/uploads
      - frameforge_prod_models:/data/models
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rmqbroker:
        condition: service_started
      qdrant:
        condition: service_started
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9090/health')"]
      interval: 5s
      timeout: 5s
      retries: 30
    mem_limit: 768m
    cpus: 0.5
    logging: *default-logging

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        DEBIAN_MIRROR: ${DEBIAN_MIRROR:-deb.debian.org}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    command: python -m frameforge.worker
    environment:
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      ROCKETMQ_NAMESERVER: ${ROCKETMQ_NAMESERVER}
      ROCKETMQ_TOPIC: ${ROCKETMQ_TOPIC}
      ROCKETMQ_CONSUMER_GROUP: ${ROCKETMQ_CONSUMER_GROUP}
      BILIBILI_IMPORT_ENABLED: ${BILIBILI_IMPORT_ENABLED}
      BILIBILI_MAX_DURATION_SECONDS: ${BILIBILI_MAX_DURATION_SECONDS}
      BILIBILI_MAX_FILE_BYTES: ${BILIBILI_MAX_FILE_BYTES}
      BILIBILI_IMPORT_ATTEMPTS: ${BILIBILI_IMPORT_ATTEMPTS}
      MAX_VIDEO_DURATION_SECONDS: ${MAX_VIDEO_DURATION_SECONDS}
      JWT_SECRET: ${JWT_SECRET}
      APP_ENV: production
      AUTO_CREATE_SCHEMA: "false"
      UPLOAD_ROOT: /data/uploads
      MAX_ANALYSIS_ATTEMPTS: ${MAX_ANALYSIS_ATTEMPTS}
      STALE_TASK_SECONDS: ${STALE_TASK_SECONDS}
      QUEUED_TASK_FALLBACK_SECONDS: ${QUEUED_TASK_FALLBACK_SECONDS:-10}
      QUEUE_WATCHDOG_INTERVAL_SECONDS: ${QUEUE_WATCHDOG_INTERVAL_SECONDS:-2}
      QUEUE_WATCHDOG_BATCH_SIZE: ${QUEUE_WATCHDOG_BATCH_SIZE:-10}
      AI_BASE_URL: ${AI_BASE_URL}
      AI_API_KEY: ${AI_API_KEY}
      AI_MODEL: ${AI_MODEL}
      AI_THINKING_MODE: ${AI_THINKING_MODE:-disabled}
      AI_REQUEST_TIMEOUT_SECONDS: ${AI_REQUEST_TIMEOUT_SECONDS:-180}
      AI_INPUT_COST_PER_MILLION_TOKENS: ${AI_INPUT_COST_PER_MILLION_TOKENS:-0}
      AI_OUTPUT_COST_PER_MILLION_TOKENS: ${AI_OUTPUT_COST_PER_MILLION_TOKENS:-0}
      AI_CACHE_HIT_INPUT_COST_PER_MILLION_TOKENS: ${AI_CACHE_HIT_INPUT_COST_PER_MILLION_TOKENS:-0}
      AGENT_MAX_TOOL_STEPS: ${AGENT_MAX_TOOL_STEPS}
      AGENT_MEMORY_MAX_MESSAGES: ${AGENT_MEMORY_MAX_MESSAGES:-12}
      AGENT_PIPELINE_VERSION: ${AGENT_PIPELINE_VERSION:-structured-v5}
      ASR_BASE_URL: ${ASR_BASE_URL}
      ASR_API_KEY: ${ASR_API_KEY}
      ASR_MODEL: ${ASR_MODEL}
      LOCAL_ASR_ENABLED: ${LOCAL_ASR_ENABLED:-false}
      LOCAL_ASR_MODEL: ${LOCAL_ASR_MODEL:-base}
      LOCAL_ASR_MODEL_ROOT: ${LOCAL_ASR_MODEL_ROOT:-/data/models}
      LOCAL_ASR_DEVICE: ${LOCAL_ASR_DEVICE:-cpu}
      LOCAL_ASR_COMPUTE_TYPE: ${LOCAL_ASR_COMPUTE_TYPE:-int8}
      LOCAL_ASR_CPU_THREADS: ${LOCAL_ASR_CPU_THREADS:-4}
      LOCAL_ASR_LANGUAGE: ${LOCAL_ASR_LANGUAGE:-zh}
      LOCAL_ASR_BEAM_SIZE: ${LOCAL_ASR_BEAM_SIZE:-5}
      LOCAL_ASR_VAD_FILTER: ${LOCAL_ASR_VAD_FILTER:-true}
      LOCAL_ASR_PRELOAD: ${LOCAL_ASR_PRELOAD:-true}
      LOCAL_ASR_RELEASE_BEFORE_OCR: ${LOCAL_ASR_RELEASE_BEFORE_OCR:-true}
      LOCAL_ASR_INITIAL_PROMPT: ${LOCAL_ASR_INITIAL_PROMPT:-}
      LOCAL_ASR_HOTWORDS: ${LOCAL_ASR_HOTWORDS:-}
      OCR_ENABLED: ${OCR_ENABLED}
      OCR_INTERVAL_SECONDS: ${OCR_INTERVAL_SECONDS:-15}
      PADDLEOCR_MODEL_ROOT: ${PADDLEOCR_MODEL_ROOT:-/data/models/paddlex}
      PADDLEOCR_MODEL_SOURCE: ${PADDLEOCR_MODEL_SOURCE:-bos}
      PADDLEOCR_DETECTION_MODEL: ${PADDLEOCR_DETECTION_MODEL:-PP-OCRv5_mobile_det}
      PADDLEOCR_RECOGNITION_MODEL: ${PADDLEOCR_RECOGNITION_MODEL:-PP-OCRv5_mobile_rec}
      PADDLEOCR_DEVICE: ${PADDLEOCR_DEVICE:-cpu}
      PADDLEOCR_CPU_THREADS: ${PADDLEOCR_CPU_THREADS:-4}
      PADDLEOCR_ENABLE_MKLDNN: ${PADDLEOCR_ENABLE_MKLDNN:-true}
      PADDLEOCR_CONFIDENCE_THRESHOLD: ${PADDLEOCR_CONFIDENCE_THRESHOLD:-0.65}
      PADDLEOCR_DEDUP_THRESHOLD: ${PADDLEOCR_DEDUP_THRESHOLD:-0.88}
      PADDLEOCR_MIN_TEXT_LENGTH: ${PADDLEOCR_MIN_TEXT_LENGTH:-1}
      PADDLEOCR_MAX_FRAMES: ${PADDLEOCR_MAX_FRAMES:-60}
      PADDLEOCR_FRAME_MAX_WIDTH: ${PADDLEOCR_FRAME_MAX_WIDTH:-960}
      PADDLEOCR_PROCESS_TIMEOUT_SECONDS: ${PADDLEOCR_PROCESS_TIMEOUT_SECONDS:-600}
      ROCKETMQ_CONSUME_THREADS: 1
      LOG_LEVEL: ${LOG_LEVEL}
    volumes:
      - frameforge_prod_uploads:/data/uploads
      - frameforge_prod_models:/data/models
    depends_on:
      api:
        condition: service_healthy
      rmqbroker:
        condition: service_started
    mem_limit: ${WORKER_MEMORY_LIMIT:-2560m}
    cpus: ${WORKER_CPU_LIMIT:-1.0}
    logging: *default-logging

  mcp:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        DEBIAN_MIRROR: ${DEBIAN_MIRROR:-deb.debian.org}
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    restart: unless-stopped
    command: python -m frameforge.mcp_server --transport streamable-http --host 0.0.0.0 --port 8001
    environment:
      FRAMEFORGE_API_URL: http://api:9090
      FRAMEFORGE_MCP_PUBLIC_URL: https://${DOMAIN}/mcp
      FRAMEFORGE_MCP_ISSUER_URL: https://${DOMAIN}/api
      LOG_LEVEL: ${LOG_LEVEL}
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 5s
      timeout: 5s
      retries: 20
    mem_limit: 256m
    cpus: 0.2
    logging: *default-logging

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: /api
        NPM_REGISTRY: ${NPM_REGISTRY:-https://registry.npmjs.org}
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    mem_limit: 192m
    cpus: 0.2
    logging: *default-logging

  caddy:
    image: caddy:2.8-alpine
    restart: unless-stopped
    environment:
      DOMAIN: ${DOMAIN}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - frameforge_prod_caddy_data:/data
      - frameforge_prod_caddy_config:/config
    depends_on:
      frontend:
        condition: service_started
      mcp:
        condition: service_healthy
    mem_limit: 192m
    cpus: 0.2
    logging: *default-logging

volumes:
  frameforge_prod_mysql:
  frameforge_prod_redis:
  frameforge_prod_qdrant:
  frameforge_prod_rmq_store:
  frameforge_prod_rmq_namesrv_logs:
  frameforge_prod_rmq_broker_logs:
  frameforge_prod_uploads:
  frameforge_prod_models:
  frameforge_prod_caddy_data:
  frameforge_prod_caddy_config:
